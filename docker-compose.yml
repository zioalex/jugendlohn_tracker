services:
  jugendlohn:
    build: .
    ports:
      - "${PORT:-5000}:${PORT:-5000}"
    environment:
      - PORT=${PORT:-5000}
    volumes:
      - jugendlohn_data:/app/data
    restart: unless-stopped

volumes:
  jugendlohn_data:
